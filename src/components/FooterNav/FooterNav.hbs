<footer class="{{styles.footer}}">
  <nav>
    <ul class="{{styles.navList}}">
      {{#each links}}
        <li><a href="{{this.href}}" class="{{../styles.link}}">{{this.label}}</a></li>
      {{/each}}
    </ul>
  </nav>
</footer>
